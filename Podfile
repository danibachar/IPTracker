# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'IDCAdBlock' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for IDCAdBlock
  pod 'Firebase/Analytics'
end

target 'IDCAdBlockControl' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for IDCAdBlockControl

end

target 'IDCAdBlockData' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for IDCAdBlockData

end
